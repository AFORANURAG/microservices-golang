{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main.go (root)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${cwd}/main.go"
        }
    ]
}
